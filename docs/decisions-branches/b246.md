← back to [docs/timeline.md](../timeline.md)

## 2026-08-18 08:38 - Correct the Departed posture to point at the version stamp, and log the L0 back-reference change

**Reasoning:** The panel found the new Departed posture told a departing repo to verify currency by recomputing computedHash with stdlib hashlib. That is false for the default path: computedHash comes from the skills.sh CLI and this repo already established its normalisation is not stdlib-reproducible -- census_counters.py tried ~40 candidate algorithms against real stored values, none matched, and CATALOG_HASH_ALGO is left None so every subscription reports indeterminate rather than a drift it cannot prove. A departing client following that guidance would fail to verify and have no way to know why. The mechanism that DOES work is #238's version: stamp, which is stdlib-reproducible by construction and was proven against real consumer trees; it merged to dev before this correction, so the doc can now point at something that exists. Verified on dev: skills_current.py present, CATALOG_HASH_ALGO None, skills carry version stamps.

**Alternatives considered:** Delete the can-still-verify paragraph rather than fix it, which would leave a departing repo with no self-check at all when one demonstrably exists. Or scope the computedHash claim to skdd's hand-authored lock that self-declares sha256_raw -- true, but useless guidance for every other consumer.

**Implications:**
- This is the two-hashes collision filed as #239 producing its first real defect: computedHash and version: answer the same question by different algorithms, and a doc reached for the wrong one. The doc now names both and says which is checkable. Also logs the #200 L0 back-reference change, which the panel correctly caught as the only commit in the batch that never ran through logmind -- AGENTS.md requires it per change, and a sibling commit satisfying check-decisions does not excuse it.

---

