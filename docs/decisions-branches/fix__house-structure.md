← back to [docs/timeline.md](../timeline.md)

## 2026-08-24 15:00 - Fix the one real house-structure gap the byte budget allows; record why the other 27 legitimately diverge

**Reasoning:** Re-measured #202 (stale: title says 29 of 56, body says 26 of 46) against today's 56-skill catalog and found 28 of 56 diverge from the dominant When to use / When NOT to use / Verification / Cross-references / Sources shape. orchestrating-elite-agent-qa was the one true oversight -- its two siblings in the 'four obligations' family (orchestrating-agent-delegation, orchestrating-a-multi-agent-run) already carry the shape, and it had 1791 bytes of headroom, so it now does too, plus a Sources line citing the same #136 build its sibling cites. token-frugal-tooling's 'Where to look for detail' section was renamed to Cross-references and its two bare skill-name mentions turned into real relative links -- a pure rename, zero new prose, confirmed by check_prose_retention as free reformatting. The other 27 stay as they are: 8 are udts-* stubs explicitly marked [L2 stub -- incubating] with no content yet to structure; 8 (api-contract-enforcement, brand-voice-review, critical-issues-only, evidence-based-review, pii-and-compliance, respect-existing-conventions, test-discipline, skill-frontmatter-quality) share placement-map.json's own family: review-discipline tag -- terse review-lens injections loaded on every PR in subscriber repos, with their own consistent internal shape; 1 (finding-a-catalog-skill) is rendered by gen_skill_directory.py and hand-editing its headers would fail --check; 1 (skillforge) is SUPERSEDED and kept unchanged during the migration window per its own frontmatter; 1 (conventions-template) is a copy-and-customize scaffold, not itself a judgment skill; 4 (composing-a-screen, consuming-a-design-system, designing-a-design-system, reviewing-design-work) are L1 pure-router dispatchers whose Sources/Verification already live one-owner-per-fact in the L0 skills they point at, and two of the four have too little headroom (7 and 56 bytes) for even a one-line note without a trim pass; curating-a-skill-catalog is missing only Sources but the smallest honest citation left only 3 bytes of margin, so it was left alone rather than mint a second razor-thin file next to test-discipline's already-known 2-byte one. The house shape itself is written down nowhere authoritative in this repo -- no CONTRIBUTING.md, no template, no validator check, no README section -- only measurable by grep.

**Alternatives considered:** Mechanically reshaping all 28: rejected because real byte headroom is far tighter than #202 assumed (several files sit 2-25 bytes under the 8192 cap), so a real fix needs a prose-trim pass across a dozen files bundled with the restructure, leaving no coherent single diff to review, Writing a new validator rule for the shape in the same pass: rejected because 27 of 28 divergences are judged deliberate here, and a rule written before the family-based exceptions are agreed with the editor would need as many carve-outs as skills

**Implications:**
- The house structure convention still has no authoritative home -- a follow-up should add a template/CONTRIBUTING note, or record the family-based exceptions in docs/placement-map.json or docs/integrating-with-agent-skills.md so a future census does not re-flag them
- curating-a-skill-catalog (84 bytes headroom) and reviewing-design-work / composing-a-screen (56 / 7 bytes) are one edit away from conforming but have no safe margin left -- a prose-trim pass would unlock them cheaply

---

## 2026-08-24 15:02 - Declare the 6-word loss check-prose-retention flagged on the token-frugal-tooling rename

**Reasoning:** Renaming '## Where to look for detail' to the house's '## Cross-references' and converting two bolded-code skill mentions into real relative links reads as pure reformatting, but the gate counts words literally: the shorter heading and the dropped, now-redundant noun 'skill' after each link net out to 6 words gone with nothing added in the same scope. The gate's own doctrine says a genuine tightening is a declared removal like any other, so it gets a row rather than a workaround -- added to docs/prose-removals.md in the prior commit's working tree but not yet committed, which is why the gate still failed on a second run against real HEAD.

**Alternatives considered:** Padding a word back in to zero out the count -- rejected: docs/prose-removals.md itself calls this a live evasion (nets the count to zero while freeing real bytes), not a fix

**Implications:**
- No content changed for a reader of token-frugal-tooling -- the two entries still say exactly what each sibling skill covers

---

## 2026-08-24 16:31 - Actually restructure token-frugal-tooling, and correct every byte number this branch produced

**Reasoning:** Panel review of #269 found two defects fixable independently of the open order-enforcement question a second, concurrent panel is now settling (#202's actual ruling covers presence only -- title 'Decided: required frame, free body' -- and a later comment on it says order is still undecided; the phrase 'a standard that isn't enforced drifts' returns zero hits repo-wide). Those two: token-frugal-tooling's earlier heading rename (renaming 'Where to look for detail' to Cross-references) never added the other four house sections it still lacked, so it read as fixed but was still divergent; it had 2703 bytes of headroom and no valid exemption (its family, catalog-and-tooling, is not collectively exempt -- sibling retiring-a-superseded-decision fully conforms), so it is now actually restructured with real When to use, When NOT to use, Verification and Sources sections, deferring detail to logmind and clud-bug-collaboration the way its Cross-references already did; it moves from divergent to conforming. Separately, every hand-computed byte number produced while working this branch undercounts by exactly one. Root cause: FRONTMATTER_RE's closing delimiter is a lookahead ((?=\n|\Z)), so m.end() lands right after the closing --- and body = content[m.end():] includes the leading newline the frontmatter block ends on; a hand-split that instead started at the next line dropped that byte. Re-measured with the gate's own regex: orchestrating-elite-agent-qa's before-fix headroom was 1790, not 1791; test-discipline's is 1, not 2; curating-a-skill-catalog's is 83, not 84 (its smallest-honest-citation leftover is 2 bytes, not 3); reviewing-design-work's is 55, not 56; composing-a-screen's and clud-bug-collaboration's are each 6, not 7; retiring-a-superseded-decision's is 6, not 7; usability-heuristics' is 12, not 13. The prior entry's 'several files sit 2-25 bytes under the cap' becomes 1-24.

**Alternatives considered:** Reasoning fully through clud-bug-collaboration, logmind and composing-a-screen's classification in this same entry -- rejected: the accounting those three lean on assumes a settled house-shape rule, and a concurrent panel found the CEO ruling that was cited for enforcing it does not exist; reasoning that would need redoing once that ruling lands is deferred rather than written twice.

**Implications:**
- token-frugal-tooling's fix stands on its own -- it adds presence, which #202's actual ruling already requires, regardless of how the order question resolves.
- clud-bug-collaboration and logmind's reasons, and composing-a-screen's reclassification, are still open; a follow-up entry finishes the accounting once the order ruling lands.
- Every byte figure in the prior two entries and the PR body should be read as off-by-one low; this entry's numbers are correct and supersede them.

---

## 2026-08-24 16:51 - Finish the #269 divergence accounting: clud-bug-collaboration was misclassified and is now fixed; logmind and composing-a-screen get checked reasons

**Reasoning:** The order ruling landed (When to use / When NOT to use / Verification / Cross-references / Sources), which only unblocks the three skills the prior entry deferred -- it does not itself supply their reasons. Measured each against the file, not the prior entry's claim. clud-bug-collaboration: placement-map.json tagged it family:review-discipline, the same tag as the 8 skills whose reason is 'terse review-lens injections, finding-shape template'. Checked all 8 siblings' actual headers (api-contract-enforcement, brand-voice-review, pii-and-compliance and test-discipline run 150-172 lines -- not terse -- but every one of the 8 shares a rubric shape: what-counts-as-X / patterns-or-categories-to-flag / what-NOT-to-flag-or-when-silent / finding-shape-template-or-how-to-phrase-findings, matching the family's own routing line verbatim ('what a review flags, what it lets go, and what a claim has to cite'). clud-bug-collaboration's 11 sections (push-fix flow, check-status semantics, strict-mode governance, skill-edit categories, workflow-edit restriction, secret handling, comment format, quiet mode, cost wiring, update cadence) never flag or withhold a finding -- zero match. It carried the tag for a real but different reason: it distributes bundled with 3 of those 8 baselines (confirmed in fix__placement-map-subscribers.md's skdd harness evidence), a distribution fact the family tag was overloaded to also mean a shape fact. Fixed rather than merely noted: reclassified to catalog-and-tooling (the same family logmind's cross-repo CLI/dev-tool procedure already sits in) in docs/placement-map.json, regenerated finding-a-catalog-skill via gen_skill_directory.py --write (diff: one bullet moves between two family sections, nothing else), re-stamped identity, regenerated skill-versions.json. logmind: authoring_home:repo-mirrored:logmind is real and checked, not a property to invoke by assertion -- docs/skill-census/2026-07-16-logmind.md S1 states the thrillmade/logmind repo copy 'IS the source' and the catalog copy 'is the distribution, synced via notify-agent-skills.yml on release'; S5/S6 record it already diverging silently once (agent-skills#131 rewrote the catalog copy for v2.0 while the upstream repo got separate edits in #178/#182, reconciled only at the next release tag); logmind-self-update.yml's own `--no-skill-install` flag exists specifically so the weekly refresh does not clobber this file. A structural edit landed here is not durable -- the next release-sync PR reconciles against the upstream file and silently reverts it, exactly the udts-stub exemption's logic ('synced from an upstream release') applied to a file with content instead of a stub. Correcting the byte number the brief carried forward: re-measured with the gate's own FRONTMATTER_RE/SIZE_LIMIT (python3 -c using the literal regex and 8192 constant from validate_skills.py lines 82/117), logmind's headroom is 52 bytes, not the 8 previously cited -- three independent methods agree (regex-based body slice, awk delimiter line, tail -n+17 byte count). Immaterial to the verdict either way: the authoring-home property, not the byte count, is what exempts it. composing-a-screen: the prior entry grouped it with 4 L1 pure-router dispatchers whose reason is 'Sources/Verification already live one-owner-per-fact in the L0 skills they point at' -- true for 3 of the 4 but false for this one, which was misclassified. Read against the file: composing-a-screen has its OWN Verification (:50, blur test/gap-ratio/axis-count/disclosure/deviations/two-gulfs) and OWN Sources (:59, Treisman & Gelade 1980/Fitts 1954/Nielsen), nothing deferred -- and LACKS When to use and Cross-references, the opposite pair. Checked whether the When-to-use gap is deliberate (frontmatter description already carries trigger language) against its true siblings in the design-dispatchers family: consuming-a-design-system, designing-a-design-system and reviewing-design-work ALL carry an explicit body '## When to use' section despite equally rich frontmatter descriptions (reviewing-design-work's runs 356 bytes) -- so the catalog's actual convention is to write it regardless, and composing-a-screen is the one family member that doesn't. Verdict: a genuine oversight, not a deliberate omission -- but it does not fit. 356 bytes for a real When-to-use section against composing-a-screen's 6-byte headroom (re-measured with the gate's own regex, matching the prior entry's number) means this is an admitted cost, not a property of the skill's job; left the file alone rather than write a stub section that would fail the reader worse than the gap does.

**Alternatives considered:** Widen the review-discipline family's stated reason in placement-map.json to also cover clud-bug-collaboration's shape -- rejected: explicitly the move the brief forbade ('do not paper the gap by widening an existing bucket's wording'), and false on the numbers -- 4 of the 8 siblings are not terse, but all 8 share the rubric shape clud-bug-collaboration lacks entirely, Restructure composing-a-screen's body now that its real gap is named -- rejected: 6 bytes of headroom cannot hold a heading, let alone the ~356-byte section its own siblings show a real one costs; would need the same dedicated trim pass already deferred for curating-a-skill-catalog and reviewing-design-work, Treat logmind's corrected 52-byte headroom as grounds to restructure it locally -- rejected: the exemption is the authoring-home property (edits here are not durable against the next release-sync PR), not the byte count; the file already diverged silently once (#131) on exactly this kind of local-only edit

**Implications:**
- docs/placement-map.json's family field was doing double duty for clud-bug-collaboration (distribution bundle AND shape bucket); the two can diverge for a skill and did here -- worth a general note in docs/integrating-with-agent-skills.md if a second case turns up
- PR #269's body table ('What was left alone, and why it is right') still states the old review-discipline reason for clud-bug-collaboration and the pre-correction byte figures for logmind/composing-a-screen; needs the same correction when the PR is next pushed -- not done here (no push, no PR edit, per this lane's brief)
- logmind and composing-a-screen's status is now fully reasoned and checked; clud-bug-collaboration's is fixed, not merely reasoned -- of the 27 divergences #202 claimed as accounted, all are now either fixed or carry a reason that survives being read against the file
- The house section order the CEO ruled on 2026-08-24 (When to use / When NOT to use / Verification / Cross-references / Sources) is cited by this lane's brief as landing on feat/house-structure-gate as an executable rule -- as of this session's `git fetch origin`, that branch is not present on origin (only origin/fix/house-structure exists under this name pattern), so it could not be verified as landed and is not asserted as such here; flag for the orchestrator to confirm before #202 is closed on the strength of it

---

