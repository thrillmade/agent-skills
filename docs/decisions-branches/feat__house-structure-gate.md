← back to [docs/timeline.md](../timeline.md)

## 2026-08-24 16:05 - Make the house section structure a real, ratcheted gate in validate_skills.py -- and pay the order debt so it ships strict

**Reasoning:** CEO ruling (2026-08-24, in session): the house section order IS enforced -- five sections, this order: When to use / When NOT to use / Verification / Cross-references / Sources. Three reasons, which are what belong in the record, not the headcount: (1) human readability across a 56-file catalog that is still growing -- a fixed sequence is worth real money in muscle memory when files are read in bulk; (2) the order runs actionable -> adjacent -> archival -- do the work, check it, find what else to read, then the footnotes, ending on the most inert material, which is where a scanner's attention drops off anyway (the alternative 9-group order puts "go read something else" before "check your work," which reads backwards); (3) all 28 conforming files already agreed on the first two sections -- the split was entirely in the tail three, so the dispute was never over the part that decides "is this even the right skill." The 14/9/5 plurality is corroboration, not the reason -- a prior version of this entry offered the count alone as the justification, which is exactly the cheapest option dressed as principle. house_structure_errors() now enforces presence AND the canonical order for every non-exempt skill, gated on docs/placement-map.json parsing cleanly. Every exemption is derived from a property already recorded elsewhere -- _is_superseded, a self-declared [L2 stub] marker, or placement-map's family/authoring_home fields -- except two (curating-a-skill-catalog, token-frugal-tooling) that could not be, and are named individually with their own reason each. Measured against the real catalog: exactly the one known real gap (orchestrating-elite-agent-qa, already being fixed on sibling branch fix/house-structure) fails; the other 27 pass through their exemption. Reordered the 14 files whose sections were complete but non-canonical (pure structural moves, zero prose change -- check_prose_retention.py's own doctrine calls reordering free) rather than opening the gate with a 14-file grandfather list, matching this repo's established precedent (#206: pay the debt, then remove the exception mechanism) of shipping a hard gate with no leniency mechanism.

**This reverts four-day-old deliberate work, and that is worth saying plainly.** PR #216 (2026-08-15) shipped `frontend-a11y`, `visual-polish` and `design-system-consistency` in the 9-file order (Cross-references before Verification), and said so explicitly: "Section ordering is split 9/9 across the catalog... I followed the brief; the catalog needs an owner's ruling, not a third variant." That was the right call in good faith, on the information available at the time -- there was no ruling yet to follow. The owner has since ruled differently, per the reasoning above, so these three files move to the canonical order along with the other 11. Following a brief and then having the brief's author rule the other way later is not a defect in the PR that followed it.

**Alternatives considered:** Presence-only, no order check: rejected -- reason 3 above (all 28 conforming files already agree on the first two sections) means the real dispute was narrow and answerable, and leaving 14 of 28 conforming files split across two non-canonical tail orders forever answers that with silence rather than a rule with teeth, Grandfather-list the 14 non-canonical files instead of reordering them: rejected -- a bare name list is a second owner of the same fact and rots, and reordering is a zero-byte, zero-prose-loss move the byte-budget constraint already anticipated ('must not require anyone to add a header to a file that cannot hold one' -- reordering adds nothing), Exempt the whole catalog-and-tooling family to cover curating-a-skill-catalog and token-frugal-tooling: rejected -- retiring-a-superseded-decision is the same family and already conforms, so a family-wide exemption would silently stop gating that skill's own future drift for the sake of two members

**Implications:**
- Three tests are RED on this branch alone, not two, and all three fail for the same single reason -- orchestrating-elite-agent-qa missing the house sections, a real gap the concurrent fix/house-structure branch already fixes: `test_entrypoint_validates_the_real_catalog_from_the_repo_root` and `test_the_real_catalog_passes_every_rule` (both tests/test_validate_skills_cli.py) and `test_the_shipped_catalog_passes_the_real_gate` (tests/test_validate_skills_version.py). Verified green in a disposable, uncommitted copy with that branch's fix applied. Merge fix/house-structure (or rebase this branch onto it) before or together with this branch to clear all three.
- Follow-up owed: curating-a-skill-catalog is 83 bytes of headroom short of an honest Sources citation -- a prose-trim pass elsewhere in the file would unlock it, same class of debt as test-discipline's 1-byte margin.
- docs/skill-versions.json regenerated against current refs (`git fetch origin` then `.github/scripts/gen_skill_versions.py --write`): two commits this branch's prior generation had cited (`03a49f8` for orchestrating-elite-agent-qa and token-frugal-tooling, `9de9a7e` for logmind) were reachable from `fix/house-structure` at generation time and are not reachable from any `refs/remotes/origin/*` ref after that branch was rebased. Regeneration replaced both with the currently-reachable commits carrying the identical blob (`8093e24`, `18d145f`) and picked up one genuinely-published row token-frugal-tooling had been missing. Every `current` digest for the 14 skills this branch reordered is byte-identical before and after regeneration -- confirmed by direct comparison, not assumed. A pre-existing, unrelated defect surfaced by the same audit: `token-naming-conventions`'s history already cited `0204f32`, unreachable from any current `refs/remotes/origin/*` ref, on `origin/dev` before this branch touched the file -- and a wider scan found 19 more pre-existing rows in the same state, all already published on `origin/dev`. Out of this branch's scope; flagged, not fixed here.

---

## 2026-08-24 16:56 - Correct the house-structure-gate record: swap an invented CEO citation for the real ruling, credit PR #216's reversal, fix two arithmetic errors, and close the two dead SHAs the version index shipped

**Reasoning:** An independent panel found the code and tests sound but the justification invented: the cited 'a standard that isn't enforced drifts... the CEO explicitly asked' quote returns zero hits repo-wide, control-tested against a phrase known to be present; the real #202 ruling settled presence only, order was still undecided. The actual CEO ruling landed today, in session, with three stated reasons (readability at catalog scale, actionable-to-archival section ordering, all 28 conforming files already agreeing on the first two sections) and the 14/9/5 plurality demoted to corroboration rather than the reason. Separately: git diff origin/dev...HEAD -- docs/skill-versions.json touched four skills this branch's SKILL.md diff never edited, citing two commits (03a49f8, 9de9a7e) reachable from fix/house-structure at generation time but from no refs/remotes/origin/* ref after that branch was rebased -- confirmed by git branch -r --contains returning empty, with a known-good SHA as a passing control. Regenerated via git fetch origin then gen_skill_versions.py --write; both replaced with the currently-reachable commits carrying the identical blob, and every current digest for the 14 reordered skills verified byte-identical before and after. Also: the decision log's own arithmetic was wrong (9+5=14, not 23) and it named two red tests where three actually fail, all for the same single cause.

**Alternatives considered:** Leave the fabricated citation in place since the underlying conclusion (order is enforced) is now independently correct: rejected -- a false citation undermines every other claim in the file even when the conclusion it supports happens to be right, and the task was explicitly to fix the authority, not just land on a true answer, Silently drop the PR #216 files into the reorder without comment: rejected -- three files reverse four-day-old deliberate work that followed a real brief in good faith; the record has to say so or it reads as though that PR was simply wrong, Fix the two newly-dead SHAs by hand-editing the JSON rather than regenerating: rejected -- gen_skill_versions.py is the one owner of that derivation and hand-editing would create a second, driftable copy of the same fact

**Implications:**
- docs/skill-versions.json regeneration surfaced 19 more pre-existing dead-SHA rows already published on origin/dev before this branch touched the file (token-naming-conventions' 0204f32 among them) -- out of this branch's scope, not fixed here, and worth its own follow-up since the append-only design means a rebased-away source branch orphans its row forever
- Neither validate_skills.py's comment nor this decision log names a byte figure that needed re-deriving under the FRONTMATTER_RE lookahead off-by-one -- checked directly: curating-a-skill-catalog's cited 83-byte headroom and token-frugal-tooling's 2000+-byte headroom both reproduce exactly against content[m.end():], the gate's own regex

---

## 2026-09-15 18:39 - Fix, don't exempt: clud-bug-collaboration and curating-a-skill-catalog now carry the real house structure; HOUSE_STRUCTURE_NAMED_EXEMPTIONS is empty

**Reasoning:** CEO ruling overrode the prior lane's byte-budget framing ('just fix the skill if you need to; condense and be judicious with words, be an editor this isnt hard'). clud-bug-collaboration had 6 bytes of headroom and only Cross-references of the five house sections; condensed redundant/duplicated prose across every existing section (merged the 'don't resolve threads' double-statement, tightened Cost-control wiring, Modifying-a-skill, Reading-review-comments) to buy room for real When to use / When NOT to use / Verification / Sources sections, reusing the existing check-status content as Verification and splitting the trailing bare URLs into Sources -- net body 7681 bytes (511 headroom), net prose loss 63 words (over FLOOR, declared in docs/prose-removals.md, verified against the gate's own Loss class). curating-a-skill-catalog was missing only Sources with 83 bytes of true headroom (re-measured with FRONTMATTER_RE, matching the branch's own prior correction); a one-line citation to this catalog's own skill-census.yml workflow and docs/skill-census/ reports fit at 82 bytes, 1 byte to spare. token-frugal-tooling's exemption was dead code -- it already carries all five sections in canonical order (fixed on this same branch's earlier commits) -- so gating it via an exemption would have silently stopped checking its own future drift. HOUSE_STRUCTURE_NAMED_EXEMPTIONS is now an empty dict with a comment stating that is the intended state, not an oversight; the mechanism itself (family/authoring_home-derived exemptions, and the named-exemption escape hatch for a file that genuinely cannot be derived from a property) is untouched, only its two occupants are gone.

**Alternatives considered:** Move the byte-tight content into references/ to buy room: rejected -- SIZE_LIMIT's own comment says a consuming reviewer reads SKILL.md and nothing else, so that move deletes the content for its reader rather than saving it. Grandfather the two skills a while longer pending a dedicated trim pass: rejected outright by the CEO ruling this branch executes.

**Implications:**
- Re-stamped identity (stamp_versions.py --write) and regenerated docs/skill-versions.json (gen_skill_versions.py --write); the only new history row introduced is finding-a-catalog-skill's eb32af1, reachable from origin/feat/technical-writing -- confirmed with git branch -r --contains, no SHA removed. 21 of 70 cited history SHAs are unreachable from any refs/remotes/origin/* ref, all pre-existing (present in docs/skill-versions.json at this branch's prior HEAD, before this change touched the file) and already flagged in a prior decisions-branches entry as its own follow-up -- not introduced or fixed here.

---

## 2026-09-15 21:22 - Restore the show_full_output flag the condensing pass cut, and fix the review-discipline docstring's stale example

**Reasoning:** Two panel findings on PR #277: clud-bug-collaboration/SKILL.md's cost-control section told a reader to check cache_read_input_tokens without the show_full_output:true flag that makes the field appear, and the prose-removals.md ledger row for that same edit asserted no actionable item was cut -- both now false in the same commit. Separately, validate_skills.py's HOUSE_STRUCTURE_EXEMPT_FAMILIES docstring cited clud-bug-collaboration as review-discipline's 'longer collaboration guide that is not a lens at all', but placement-map.json moved that skill to catalog-and-tooling in #269 and it now fully conforms to house structure anyway, so the example proved nothing.

**Alternatives considered:** Amend the ledger row's declaration sentence instead of restoring the flag -- rejected because the reader needs the flag to follow the instruction at all, and restoring cost only bytes the file had headroom for, Drop the review-discipline docstring's long-vs-short contrast entirely instead of replacing the example -- rejected because the contrast is still true (terse flag-only skills vs full lenses with worked categories) and removing it would weaken a still-valid argument for the exemption

**Implications:**
- prose-removals.md's clud-bug-collaboration row net-word count changes from 63 to 61 words (re-measured with check_prose_retention.Loss against the merge-base with origin/dev, 0bb41be9) now that the flag is back; the rest of that row's declared items were independently re-checked against the merge-base diff and hold
- validate_skills.py's review-discipline example now cites test-discipline and pii-and-compliance, which are still in that family per placement-map.json and still lack the house shape (When to use/When NOT to use), unlike guarding-a-regression and proving-an-absence already cited as the family's exceptions

---

